package abstractpkg;

public interface InterfaceEg 
{
	int a=10; //As variables are static final in interface,assignment should done along with declaration
	void print1();  //As interface holds only abstract methods,end with semicolon & no definition
}
